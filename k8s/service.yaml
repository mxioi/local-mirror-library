apiVersion: v1
kind: Service
metadata:
  name: archive-service
  namespace: local-mirror
spec:
  selector:
    app: archive-app
  type: ClusterIP
  ports:
    - name: web
      port: 8080
      targetPort: web
    - name: api
      port: 8010
      targetPort: api
